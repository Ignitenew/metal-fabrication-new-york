services:
  - type: web
    name: metal-fabrication-new-york
    env: static
    buildCommand: mkdir -p public && cp -r *.html assets sitemap.xml public/ 2>/dev/null; cp -r *.html public/ 2>/dev/null; true
    staticPublishPath: public
